# Claude Code 개인 설정 (각 PC마다 다름, 공유 안 함)
.claude/settings.local.json

# OS 부산물
Thumbs.db
.DS_Store

# 사진 원본 폴더 (폰/카메라 원본, 합계 400MB+)
# → 사이트에 쓰는 사진은 웹용으로 압축해 루트에 커밋되어 있음.
#   원본까지 올리면 저장소가 비대해지고 Vercel 배포 용량 한도에 걸림.
커피음료/
홈페이지 사진/
홈페이지 사진모음/
